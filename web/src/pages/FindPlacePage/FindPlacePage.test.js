import { render } from '@redwoodjs/testing/web'

import FindPlacePage from './FindPlacePage'

describe('FindPlacePage', () => {
  it('renders FindPlacePage successfully', () => {
    render(<FindPlacePage />)
  })
})
